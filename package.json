{
	"name": "cloudflare-html-rewrite-example",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev:local": "wrangler dev --live-reload --env dev",
		"dev:remote": "wrangler dev --remote --env dev",
		"deploy:staging": "wrangler deploy --env dev",
		"deploy:production": "wrangler deploy --env production"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240117.0",
		"typescript": "^5.0.4",
		"wrangler": "^3.28.1"
	},
	"dependencies": {
		"node-html-parser": "^6.1.12"
	}
}
